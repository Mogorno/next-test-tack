import { Todos } from '@/widgets/todos';

const TodosPage = () => {
    return (
        <div className="mt-24 container mx-auto w-10/12">
            <Todos />
        </div>
    );
};

export default TodosPage;
